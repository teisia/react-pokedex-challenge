// table columns
export const columns = ['Name', 'Num', 'Type', 'Weaknesses']

// table filter categories
export const filterCategories = ['Type', 'Weaknesses']

// data index for filter
export const dataIndexes = { type: 0, weaknesses: 1}